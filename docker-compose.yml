version: "3.3"
services:
  dev_rn_task_explorer:
    image: streamlit-template-devapp
    build:
      dockerfile: Dockerfile
      context: .
      target: streamlit-template-devapp
    environment:
      PORT: 5000
    volumes:
      - ./app:/app/app
    ports:
      - "5050:5000"
  rn_task_explorer:
    image: app
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      PORT: 5000
    ports:
      - "5055:5000"
